apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://cloud-metrics:9090
    isDefault: true
    editable: false

  - name: Loki
    type: loki
    access: proxy
    url: http://cloud-logs:3100
    editable: false
